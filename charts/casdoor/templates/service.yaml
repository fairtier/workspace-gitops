apiVersion: v1
kind: Service
metadata:
  name: casdoor
  labels:
    app: casdoor
spec:
  selector:
    app: casdoor
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
